name: jq

on:
  workflow_dispatch:

jobs:
  jq:
    if: true
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: ECHO
        run: |
          echo '{"foo1": "bar1", "foo2": "bar2"}' | jq '.foo1'
